<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :emails_contact %><br>
    <%= f.text_field :emails_contact %>
  </div>
  <div class="field">
    <%= f.label :country_contact %><br>
    <%= f.text_field :country_contact %>
  </div>
  <div class="field">
    <%= f.label :phones_contact %><br>
    <%= f.text_field :phones_contact %>
  </div>
  <div class="field">
    <%= f.label :organization_id %><br>
    <%= f.number_field :organization_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
